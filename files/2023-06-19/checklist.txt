/. no hidden layers left
/. all lines must = 1px
/. all colors must = fff or ffffff
/. all caps round; all corners round
/. no +-.,! in SVG file names
/. all appfilter lines match new icon names
7. no "transform", "fill-rule:evenodd", "scientific e-notation" in svg

regex
stroke-width[:=]"?\d*.?\d*"?
stroke-width="1"

stroke="#[abcdef0-9]{3,6}"
stroke="#fff"

stroke-linecap="[a-z]*"
stroke-linecap="round"

stroke-linejoin="[a-z]*"
stroke-linejoin="round"
